/**
 * @deprecated LEGACY — blocked to prevent direct Dexie access.
 * Use useExamsBridge instead.
 */

export function useExams(): never {
  throw new Error(
    'useExams is legacy and disabled. Use useExamsBridge instead.'
  )
}
