/**
 * Legacy demo app entrypoint.
 *
 * The old seeded demo flow has been retired. Demo data is now imported via the
 * teacher UI using the shared CSV import pipeline.
 */

console.log('ViccoBoard RC mode');
console.log('The legacy seeded demo app has been retired.');
console.log('Use the teacher UI and the "RC-Demo-CSV-Paket laden" action instead.');
